name: Deploy Javadoc

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy JavaDoc 🚀
        uses: MathieuSoysal/Javadoc-publisher.yml@v2.4.0
        with:
          GITHUB_TOKEN: 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
          javadoc-branch: javadoc
          java-version: 17
          target-folder: javadoc 
          project: gradle